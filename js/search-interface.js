var Repos = require(. / .. / js / search.js).reposModule;

$(document).ready(function() {
  $('#btn').click(function() {
    var name = $('#name')
    // return (name)
    console.log(name)
  });
});
